# LLM Provider Credentials
# Stepfun is the only supported LLM provider in the current production path.
# There is intentionally no alternate-provider routing and no fallback response path.

STEPFUN_BASE_URL=https://api.stepfun.ai/step_plan/v1
STEPFUN_API_KEY=your-stepfun-api-key-here
STEPFUN_MODEL=step-3.7-flash
