# INVENTORY — prime-agent files >= 100 LOC

> Generated by `scripts/meta_loop.py inventory`. Scope: 302 files >= 100 LOC across 12 areas.
> `responsibility` and `verdict` are filled by the digest loop; a row with either empty FAILS the L1 gate.
> verdict vocabulary: `adopt` | `adapt` | `reject` | `n/a` (with a reason).
>
> **SCOPE LIMITATION, recorded because it cost us real coverage.** The >=100 LOC threshold is
> a proxy for importance, and it is a leaky one. Three of the strongest mechanisms found in
> this digest come from files this inventory EXCLUDES:
>   - `packages/ai/src/api-registry.ts` (98 LOC) — provider registry + api-mismatch assertion (M27)
>   - `packages/ai/src/cache-pricing.ts` (63 LOC) — Anthropic cache cost model
>   - `packages/ai/src/utils/sanitize-unicode.ts` (25 LOC) — unpaired-surrogate stripping (M28)
> `packages/ai/src/stream.ts` (59 LOC) is likewise the entire public streaming seam.
> Density is not size: a 25-line file can carry a correctness invariant that a 3000-line
> UI file does not. Do not read 100% L1 coverage as "the codebase is understood".
>
> **Rows whose `responsibility` begins with `Exports:` were auto-extracted from the export
> list, NOT read.** They are honest placeholders. A verdict must not be assigned to them
> without reading the file, since a transfer judgement inferred from symbol names is a guess
> wearing the costume of a finding.

## AGENTS.md (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `AGENTS.md` | 259 | Engineering discipline: `npm run check` pre-commit gate, faux-provider-only tests, `<issue>-<slug>` regression naming, lockstep versioning, forbidden git ops for parallel agents. | adopt |

## README.md (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `README.md` | 108 | Product thesis: RLM (prompt-as-variable, programmatic subagent calls) + Continual Harness (durable refinable supplemental state, immutable base prompt). | adapt |

## packages/agent (6 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `src/agent-loop.ts` | 986 | Outer/inner agent loop over AgentMessage, converting to provider Message[] only at the LLM call boundary; owns tool-call iteration and abort. | adapt |
| `src/agent.ts` | 613 | Agent facade holding model/tool/retry config (maxRetries, maxRetryDelayMs, timeoutMs) and threading it into each stream call. | adapt |
| `CHANGELOG.md` | 541 | UNREAD — release history; lockstep-versioning artifact only, no design rationale. | n/a |
| `README.md` | 501 | UNREAD — outside the assigned area file-list; agent-package API surface reference. | n/a |
| `src/types.ts` | 421 | Agent-side type contracts: AgentMessage variants, loop hook signatures and stop-reason vocabulary. | adapt |
| `src/proxy.ts` | 367 | Proxy stream function letting an app route LLM calls through its own server, which owns auth; forwards a whitelisted option subset. | adopt |

## packages/ai (31 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/generate-models.ts` | 2417 | Build-time generator that emits the models catalog; the generated file must never be hand-edited. | n/a |
| `CHANGELOG.md` | 1403 | UNREAD — release history for the AI package. | n/a |
| `README.md` | 1332 | UNREAD — provider/model SDK reference for the TS AI package, no governance content expected. | n/a |
| `src/providers/openai-codex-responses.ts` | 1332 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/anthropic.ts` | 1279 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/openai-completions.ts` | 1163 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/amazon-bedrock.ts` | 984 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/mistral.ts` | 639 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/openai-responses-shared.ts` | 562 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/google-vertex.ts` | 545 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/faux.ts` | 499 | Scripted fake provider returning pre-built assistant messages/tool calls, enabling the whole suite to run with no API key or cost. | adopt |
| `src/types.ts` | 471 | Shared provider contract: StreamOptions incl. timeoutMs/maxRetries/maxRetryDelayMs, Model/Api types and the unified event vocabulary. | adopt |
| `src/providers/google.ts` | 468 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/utils/oauth/openai-codex.ts` | 458 | Vendor-specific OAuth login/refresh flow. | reject |
| `src/providers/register-builtins.ts` | 403 | Registers every built-in provider into the api registry at import time. | adapt |
| `src/utils/oauth/anthropic.ts` | 402 | Vendor-specific OAuth login/refresh flow. | reject |
| `src/utils/oauth/github-copilot.ts` | 396 | Vendor-specific OAuth device-code flow. | reject |
| `src/mcp/oauth.ts` | 380 | OAuth flow for MCP servers: token acquisition, storage and refresh. | adapt |
| `src/providers/google-shared.ts` | 376 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/utils/validation.ts` | 324 | Validates a tool call against its declared schema before execution, throwing a formatted error on unknown tool or bad arguments. | adopt |
| `src/providers/openai-responses.ts` | 302 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/providers/azure-openai-responses.ts` | 288 | Vendor stream adapter: maps the neutral request onto one vendor SDK/wire API and normalises its events back; retry is delegated to the vendor SDK. | adapt |
| `src/env-api-keys.ts` | 234 | Resolves provider API keys from environment variables by provider id. | adapt |
| `src/utils/stream-failure.ts` | 232 | Nine-kind failure taxonomy; classifies by error TEXT before status so a safety refusal wearing a 5xx is not retried (M25). | adopt |
| `src/providers/transform-messages.ts` | 220 | Converts the neutral message model into vendor wire formats. | adapt |
| `src/utils/overflow.ts` | 153 | Context-overflow detection: 21 provider patterns, a NON_OVERFLOW subtraction guard, plus silent- and length-stop overflow cases (M26). | adopt |
| `src/utils/oauth/index.ts` | 152 | Barrel re-exporting the OAuth helpers. | n/a |
| `src/cli.ts` | 133 | Small CLI entry for the ai package. | reject |
| `src/utils/json-parse.ts` | 124 | Repairs and partially parses malformed/truncated JSON from a model stream so a mid-stream cut does not lose the whole tool call. | adopt |
| `src/utils/oauth/oauth-page.ts` | 115 | Serves the local browser callback page for an OAuth redirect. | reject |
| `src/models.ts` | 108 | Model lookup helpers over the generated catalog. | adapt |

## packages/coding-agent (231 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `src/core/agent-session.ts` | 11208 | Mode-independent agent lifecycle core: turn execution, compaction, goal/token budget accounting, autonomous continuation gates, RLM subagent runs, queued-action recovery. | adapt |
| `src/modes/interactive/interactive-mode.ts` | 9728 | TUI interactive mode: renders the chat surface, splash header, queued-message previews, and delegates agent execution to an AgentConnection. | reject |
| `src/modes/daemon/daemon-mode.ts` | 6793 | Background daemon owning live session runtimes behind a JSONL socket protocol, with session leases, attach/detach, passivation/hydration and a worker recovery journal. | adapt |
| `src/modes/daemon/daemon-supervisor.ts` | 4872 | Supervisor process that spawns, adopts, recovers and idle-evicts daemon workers, and drives fenced zero-loss update-restart with a command idempotency journal. | adopt |
| `src/modes/agents-view/agents-view-mode.ts` | 2942 | Fullscreen TUI dashboard listing running/idle agents, with scope frames for drilling into subagent trees and daemon reconnect handling. | reject |
| `docs/extensions.md` | 2589 | The full extension surface: event lifecycle, blockable `tool_call`, middleware-chained `tool_result`, state-in-details persistence, file-mutation queue, custom tools/UI. | adapt |
| `src/core/package-manager.ts` | 2475 | Resolves and installs extension/skill sources from npm, git and local paths into user/project scopes, and tracks installed paths for updates. | reject |
| `src/core/session-manager.ts` | 2324 | Append-only JSONL session store: entry tree with branching, compaction/branch-summary entries, versioned on-disk format with in-place migrations, and context reconstruction. | adopt |
| `src/modes/agent-connection/daemon-agent-connection.ts` | 2092 | Client adapter mapping the generic agent-connection interface onto daemon socket commands, handling reconnect, event replay cursors and capability probing. | adapt |
| `src/core/cron-jobs.ts` | 1736 | Durable scheduled-job and heartbeat store: schedule parsing, next-run computation, claim-before-deliver dispatch, and interrupted-dispatch recovery. | adopt |
| `src/main.ts` | 1702 | CLI entry point: parses argv, resolves run mode and session selector, ensures/takes over the daemon, then delegates to a mode runner. | reject |
| `src/cli/daemon-command.ts` | 1691 | Dispatches the daemon subcommand surface (start/stop/status/session ops) against a running daemon. | adapt |
| `src/package-manager-cli.ts` | 1641 | CLI for install/remove/update/list of agent packages, including a daemon update-restart coordinator that drains and re-launches live sessions across an upgrade. | adapt |
| `src/core/model-registry.ts` | 1605 | Registry of built-in and custom models with API-key resolution, provider registration, and per-provider auth staleness tokens. | adapt |
| `src/core/kernel/index.ts` | 1529 | Manages the persistent IPython kernel: ZeroMQ Jupyter wire protocol with HMAC signing, execute/interrupt lifecycle, namespace snapshot-restore and disposal. | adapt |
| `src/core/extensions/types.ts` | 1523 | Type surface for the extension system: lifecycle event shapes, tool/command/flag registration, and a large terminal-UI context interface. | reject |
| `docs/rpc.md` | 1456 | Line-delimited JSON command/event protocol; steering vs follow-up queue modes (L360-395) are the transferable part, the rest is TS wire detail. | adapt |
| `src/modes/interactive/theme/theme.ts` | 1365 | Terminal color theme model: loading, validation, ANSI/RGB resolution, live file watching, and export color mapping. | reject |
| `examples/extensions/overlay-qa-tests.ts` | 1348 | Comprehensive overlay QA suite: anchors, margins, stacking, overflow, ~30 FPS animation; the "ship a QA-harness extension" idea is transferable, the content is not. | reject |
| `src/core/settings-manager.ts` | 1277 | Layered global/project settings with lock-guarded read-modify-write persistence, typed schema, and override application. | adapt |
| `src/cli/daemon-ps.ts` | 1261 | Discovers every daemon on the machine by merging OS socket→pid listings with a socket-dir sweep, then probing each. | adapt |
| `src/modes/interactive/components/tree-selector.ts` | 1242 | TUI component for browsing the session message tree with filter modes (no-tools, user-only, labeled-only). | reject |
| `src/modes/daemon/daemon-protocol.ts` | 1172 | Wire protocol between daemon and clients: versioned envelopes, event sequences/cursors, and explicit client/server capability sets. | adopt |
| `src/core/auth-storage.ts` | 1135 | Credential store for API keys and OAuth tokens with file locking to serialize concurrent token refresh across processes. | adapt |
| `docs/sdk.md` | 1123 | Embedding API; `PromptOptions.streamingBehavior` steer/followUp and the `preflightResult` accept-vs-succeed split (L184-240) are the reusable ideas. | adapt |
| `src/core/extensions/runner.ts` | 1066 | Executes loaded extensions: dispatches lifecycle events, aggregates registered tools/commands/shortcuts, and isolates extension errors from the host. | adapt |
| `src/core/refinement/refinement.ts` | 1017 | The "continual harness": persistent editable prompt notes, memories, skills and subagent specs, with scoped state, append-only refinement history and rollback. | adopt |
| `examples/extensions/tic-tac-toe.ts` | 1008 | Demonstrates `executionMode: "sequential"` on a tool to prevent parallel tool calls racing on shared cursor state. | adapt |
| `src/core/agent-traces.ts` | 968 | Uploads session trace files to a remote endpoint with debouncing, bounded retries with jittered backoff, client-side rate limiting, and bounded concurrency. | adapt |
| `src/modes/agents-view/agents-view-state.ts` | 968 | Pure state model for the agents dashboard: classifies sessions into running/idle/inactive sections and reconciles daemon and on-disk session records. | adapt |
| `examples/extensions/subagent/index.ts` | 955 | Subprocess-per-subagent delegation in single/parallel/chain modes with streaming, usage accounting, abort propagation, max 8 tasks and 4 concurrent. | adapt |
| `src/core/resource-loader.ts` | 938 | Discovers and loads skills, prompts, themes, extensions, and context files from project/user/bundled scopes, reporting collision diagnostics. | adapt |
| `src/core/kernel/bootstrap.ts` | 929 | Provisions the Python kernel venv (uv, ipykernel, runtime deps), with a content-hashed schema-versioned bootstrap marker and cross-process bootstrap locking. | adapt |
| `src/modes/interactive/auth-flows.ts` | 923 | TUI login/logout dialogs and provider selectors shared by interactive mode and the agents view. | reject |
| `docs/tui.md` | 919 | Component interface and building blocks for terminal rendering; product-shape specific to a TS terminal UI. | reject |
| `src/core/compaction/compaction.ts` | 867 | Context compaction: token estimation, threshold policy, turn-aligned cut-point selection, summary generation, and file-operation carry-forward. | adopt |
| `src/modes/daemon/daemon-supervisor-ownership.ts` | 807 | Durable single-owner election for the daemon supervisor: lock-guarded registry records, pid+process-start-id identity, startup fences, and shutdown admission leases. | adopt |
| `src/core/agent-session-runtime.ts` | 802 | Owns a session's lifecycle: acquires the session lease, hosts subagent runtimes, rebinds on session replacement, and flushes traces on shutdown. | adopt |
| `src/core/telemetry.ts` | 797 | Batched analytics client with a persisted installation id, opt-out check, and event capture hooks installed onto a session. | adapt |
| `src/modes/interactive/components/ipython-cell.ts` | 749 | TUI renderer for IPython tool cells: code preview, diff rendering, and cached wrapped output. | reject |
| `src/modes/agent-connection/types.ts` | 745 | Type definitions for the agent-connection abstraction: session entry variants, snapshots, and command/event payload shapes. | n/a |
| `src/core/prime-inference-auth.ts` | 736 | Prime Inference/Traces auth: browser and CLI login flows, RSA-wrapped key exchange, team selection, and atomic 0600 credential file writes. | adapt |
| `src/core/tools/ipython.ts` | 708 | Owns lazy create/start/bootstrap of a session's IPython kernel and the ipython tool definition. | adopt |
| `CHANGELOG.md` | 707 | UNREAD — release history; the one-line past-tense bullet format under `[Unreleased]` is specified in AGENTS.md L105-133. | n/a |
| `README.md` | 702 | CLI reference; read only the autonomous/goal/schedule flag tables (L514-519, L601-614) giving bounded-run defaults and per-gate retry/timeout semantics. | adapt |
| `src/modes/daemon/daemon-client.ts` | 692 | Daemon socket client: request/response correlation, capability gating with a typed unavailable-capability error, auto-reconnect, and in-flight request recovery. | adopt |
| `src/core/model-resolver.ts` | 688 | Resolves model patterns to concrete models, applies scoping globs, and picks the initial model from CLI, session, or provider defaults. | adapt |
| `src/modes/rpc/rpc-client.ts` | 671 | Client that spawns the agent subprocess and correlates JSON-RPC requests to responses with per-request timeouts. | adopt |
| `src/config.ts` | 664 | Package-level paths, app naming, version, install-method detection, and self-update command construction. | reject |
| `docs/custom-provider.md` | 646 | How extensions register providers/OAuth/custom streaming; TS-provider-registry specific with no governance content. | reject |
| `src/modes/agent-connection/in-process-agent-connection.ts` | 640 | In-process implementation of the AgentConnection interface, adapting a local AgentSessionRuntime to the same surface the daemon client exposes. | adapt |
| `src/core/agent-messages.ts` | 636 | Agent-to-agent messaging: family reachability rules (parent/sibling/child only), per-session pending caps, token-bucket rate limits, and delivery receipts. | adopt |
| `src/core/skills.ts` | 633 | Loads markdown and python skills from scoped directories with frontmatter parsing, ignore-file support, name validation, and prompt formatting. | adopt |
| `examples/rpc-extension-ui.ts` | 632 | Reference client spawning the agent in RPC mode and servicing extension UI requests — proves the UI/execution split is really a protocol. | adapt |
| `src/core/extensions/loader.ts` | 605 | Loads TypeScript extension modules via jiti, builds the extension runtime/API surface, and discovers extensions in local and global directories. | adapt |
| `examples/extensions/custom-provider-anthropic/index.ts` | 604 | Full custom provider with OAuth login flow and bespoke streaming; TS provider-registry plumbing. | reject |
| `src/modes/interactive/components/config-selector.ts` | 603 | TUI component for enabling/disabling installed package resources. | reject |
| `src/cli/daemon-update-restart.ts` | 602 | Coordinates self-update daemon restart via a locked coordinator registry and a versioned status file. | adopt |
| `src/core/autonomous.ts` | 593 | Autonomous continuation control: continuation/turn/token/time budgets, limit-reason reporting, and quality gate commands with bounded retries. | adopt |
| `src/cli/daemon-launch.ts` | 581 | Probes daemon liveness/version, replaces stale daemons safely, and waits for readiness or clean exit. | adopt |
| `examples/extensions/space-invaders.ts` | 560 | Space Invaders using Kitty press/release detection for smooth movement; demo. | reject |
| `src/modes/rpc/rpc-mode.ts` | 553 | Headless RPC mode: JSON commands on stdin, responses and events on stdout. | adapt |
| `src/cli/owned-session-worker.ts` | 540 | Runs a session in an owned child process, tracking a crash-recovery descriptor and killing the worker when its owner dies. | adopt |
| `src/modes/interactive/components/model-selector.ts` | 527 | TUI fuzzy-filtered model picker overlay. | reject |
| `src/modes/interactive/components/settings-selector.ts` | 527 | TUI settings overlay wiring toggles and choices to settings callbacks. | reject |
| `src/modes/interactive/components/tool-execution.ts` | 520 | TUI renderer for tool invocations and their results, including extension-supplied custom renderers. | reject |
| `src/modes/daemon/daemon-catalog-process.ts` | 511 | Runs session-catalog listing and deletion in a separate child process, with a client that spawns and speaks a small message protocol to it. | adapt |
| `src/core/tools/edit.ts` | 503 | Defines the edit tool over a pluggable EditOperations interface for local or remote file mutation. | adopt |
| `src/modes/daemon/daemon-session-list.ts` | 493 | Builds the session roster: durable lifecycle (draft/live/archived), activity classification, and recursive subagent snapshot assembly. | adapt |
| `src/core/messages.ts` | 484 | Custom message types (compaction summary, branch summary, heartbeat, subagent failure notices) and their conversion into LLM-visible text. | adapt |
| `docs/models.md` | 474 | `models.json` schema for custom providers/models, per-model overrides and API compatibility notes; TS-config specific. | reject |
| `src/core/extensions/builtin/herdr-agent-state.ts` | 461 | Built-in extension reporting agent working/idle/blocked state to the Herdr terminal workspace over a Unix socket. | reject |
| `src/core/tools/bash.ts` | 456 | Defines the bash tool with a pluggable operations interface so execution can be delegated to remote backends. | adopt |
| `docs/session-format.md` | 455 | The JSONL tree v3 schema: `id`/`parentId` entries, ~14 entry kinds, compaction/branch_summary, `buildSessionContext()` walk, bookkeeping exclusion rules. | adopt |
| `src/core/tools/edit-diff.ts` | 448 | Shared diff computation with progressive normalization for fuzzy matching of edit targets. | adopt |
| `src/core/tools/code-preview.ts` | 443 | Produces syntax-aware previews of bash/python/ipython code for display. | reject |
| `src/cli/public-command.ts` | 435 | Routes top-level public CLI commands and returns a structured result. | adapt |
| `src/modes/interactive/components/menu-panel.ts` | 433 | TUI menu panel primitives: viewport layout, search input, rows, and scrolling list. | reject |
| `src/modes/interactive/components/oauth-selector.ts` | 431 | TUI provider/service picker for OAuth and API-key login. | reject |
| `examples/extensions/preset.ts` | 430 | Named JSON presets bundling model + thinking level + active tools + prompt instructions, merged project-over-global, switchable at runtime. | adapt |
| `examples/extensions/questionnaire.ts` | 427 | Multi-question tool with tab-bar navigation between questions; the tool-asks-the-human shape maps to HUMAN_CHECKPOINT, the UI does not. | adapt |
| `src/modes/acp/acp-mode.ts` | 427 | Runs ACP mode: capability negotiation on initialize plus session new/prompt/close/cancel handlers. | adopt |
| `src/migrations.ts` | 423 | One-time startup migrations: consolidate legacy credential files into auth.json and relocate session directories to the new layout. | adapt |
| `src/core/sdk.ts` | 409 | Public SDK entry that assembles an AgentSession from auth, model registry, extensions, MCP, and tools; also re-exports the runtime surface. | adapt |
| `src/core/session-action-store.ts` | 399 | Session action queue: explicit lifecycle state machine with a legal-transition table, rollback proof requirements, and passivation/eviction predicates. | adopt |
| `src/index.ts` | 397 | Public package barrel re-exporting the agent's types and classes. | n/a |
| `docs/compaction.md` | 395 | Compaction policy: threshold `contextTokens > contextWindow - reserveTokens`, cut-point rules, split turns, structured summary schema, cumulative file tracking. | adopt |
| `docs/skills.md` | 392 | Progressive disclosure: only name+description in the system prompt, full `SKILL.md` loaded on match; Python-backed skill detection contract and precedence rules. | adopt |
| `src/utils/tools-manager.ts` | 384 | Ensures managed binaries (fd, rg) exist, downloading on demand and classifying why they are unavailable. | adopt |
| `src/modes/interactive/components/armin.ts` | 382 | Animated XBM easter-egg component. | reject |
| `src/modes/daemon/daemon-session-summarizer.ts` | 381 | Generates short agent status lines with a small model, debouncing rapid turn-end bursts and sweeping sessions on an interval. | adapt |
| `docs/usage.md` | 376 | Autonomous-mode option table with exact defaults and the gate-then-limit evaluation order (L258-295); the most precise bounded-run spec in the repo. | adopt |
| `src/modes/interactive/components/prime-onboarding-splash.ts` | 375 | Animated onboarding splash rendering the Prime logo in the terminal. | reject |
| `src/core/keybindings.ts` | 368 | App keybinding definitions, config file loading, and a migration for renamed keybinding ids. | reject |
| `src/core/kernel/fork-server.ts` | 363 | Node-side client for a Python forkserver that forks pre-imported kernels, with profile keying on (python, cwd, env) and fallback to direct spawn. | adapt |
| `src/core/compaction/branch-summarization.ts` | 360 | Summarizes a conversation branch being left during tree navigation so its context survives the jump, under a token budget. | adopt |
| `src/modes/interactive/components/scoped-models-selector.ts` | 358 | TUI editor for the model scope pattern list. | reject |
| `src/modes/rpc/rpc-types.ts` | 356 | Type-only definitions of the RPC command/response/event union surface. | n/a |
| `src/cli/command-registry.ts` | 352 | Declarative registry of command specs with public/removed name sets and subcommand lookup. | adapt |
| `src/modes/interactive/components/login-dialog.ts` | 350 | TUI dialog collecting API keys or launching OAuth for a chosen provider. | reject |
| `examples/extensions/custom-provider-gitlab-duo/index.ts` | 349 | GitLab Duo gateway provider delegating to built-in streaming implementations; provider plumbing. | reject |
| `src/cli/args.ts` | 346 | Parses CLI arguments into a typed Args record with structured diagnostics for invalid/removed flags. | adapt |
| `examples/extensions/snake.ts` | 343 | Snake game with custom UI, keyboard handling and session persistence; demo. | reject |
| `examples/extensions/plan-mode/index.ts` | 340 | Read-only exploration mode: swaps the active tool set, allowlists bash, extracts numbered plan steps and tracks `[DONE:n]` completion. | adapt |
| `docs/settings.md` | 338 | Project-overrides-global JSON settings with a documented key table; the two-layer precedence idea generalizes, the keys do not. | adapt |
| `src/core/context-tree.ts` | 324 | Builds the agent context tree, computing own vs. total token usage per node and loading subagent nodes from disk when not resident. | adopt |
| `src/modes/interactive/components/heartbeat-manager.ts` | 323 | TUI panel for viewing and managing a session's heartbeat cron jobs. | reject |
| `examples/extensions/sandbox/index.ts` | 321 | OS-level sandbox (sandbox-exec/bubblewrap) with merged global+project JSON policy: denyRead ~/.ssh, allowWrite, denyWrite .env, domain allowlist. | adopt |
| `src/core/agent-session-services.ts` | 314 | Constructs the shared service bundle (auth, settings, models, MCP, resources, telemetry, traces) that sessions and subagents are built from. | adapt |
| `src/core/export-html/index.ts` | 314 | Exports a session transcript to a standalone themed HTML file, including extension-rendered tool output. | reject |
| `src/core/footer-data-provider.ts` | 311 | Watches git HEAD and related files to keep branch/dirty-state footer data fresh without polling git on every render. | reject |
| `src/modes/acp/acp-events.ts` | 309 | Pure mapping from internal agent session events to ACP session updates and tool kinds/statuses. | adopt |
| `src/modes/interactive/components/custom-editor.ts` | 301 | TUI editor subclass extending the base Editor with app keybindings, cursor markers, and width-aware truncation. | reject |
| `src/utils/clipboard-image.ts` | 300 | Reads an image from the OS clipboard across Wayland/X11/WSL with a PowerShell fallback for Windows screenshots. | reject |
| `src/core/prompt-templates.ts` | 299 | Loads markdown prompt templates from user/project dirs, parses frontmatter, substitutes positional/named args, and expands slash-command references. | adapt |
| `docs/themes.md` | 297 | JSON theme token format for TUI colors; presentation-only. | reject |
| `examples/extensions/todo.ts` | 297 | Canonical state-in-tool-result-`details` pattern: state is rebuilt by walking the branch, so it forks and branches correctly for free. | adopt |
| `src/core/kernel/state-snapshot.ts` | 297 | Builds and parses the Python code that pickles/revives the kernel user namespace so variables survive session resume. | adapt |
| `src/modes/interactive/components/assistant-message.ts` | 296 | TUI container rendering assistant messages as markdown with collapsible error details and auth-recovery hints. | reject |
| `src/modes/interactive/components/diff.ts` | 294 | Renders unified diffs to ANSI terminal strings with syntax highlighting, line numbers, and width-aware wrapping. | reject |
| `src/core/goals.ts` | 290 | Defines persistent thread-goal state (objective, status, token budget) with validation, usage accounting, and goal-context message construction. | adopt |
| `src/modes/daemon/snapshot-transcript-cache.ts` | 290 | Chunked transcript snapshot cache that spills from memory to disk past a byte threshold and serves chunks to readers with refcounted disposal. | adapt |
| `src/core/session-lease.ts` | 285 | Cross-process exclusive session ownership via atomic directory-rename leases, PID+process-start-id liveness checks, and stale-lease reclamation. | adopt |
| `src/core/slash-commands.ts` | 275 | Parses and resolves slash commands from text, distinguishing builtin, extension, prompt, and skill sources with argument options. | adapt |
| `src/utils/git.ts` | 272 | Parses git URLs/shorthand and locates git metadata paths by walking up from cwd. | adapt |
| `docs/rlm-runtime.md` | 269 | Host-request bridge over a control channel, depth limits, child registry survival, `child_usage_attributed` accounting, artifact layout, trust boundary, failure table. | adopt |
| `src/modes/daemon/daemon-socket.ts` | 267 | Acquires and prepares the daemon's unix socket path under a lockfile, restricts permissions to 0600, and cleans up stale sockets. | adapt |
| `src/core/tools/truncate.ts` | 265 | Head/tail truncation to line and byte budgets that never emits partial lines, plus per-line clamping. | adopt |
| `examples/extensions/question.ts` | 264 | Single-question custom UI with options list plus inline free-text editor and escape semantics; terminal UI. | reject |
| `skills/attach-image/src/attach_image/attach_image.py` | 258 | Python-backed skill converting local/remote images into attachable content via a host request. | reject |
| `src/core/export-html/ansi-to-html.ts` | 258 | Converts ANSI escape sequences (16/256/RGB color, bold, italic) into inline-styled HTML spans. | reject |
| `src/modes/interactive/components/configuration-menu.ts` | 258 | Tabbed TUI menu for editing providers, models, and MCP connections. | reject |
| `src/modes/daemon/daemon-extension-binding.ts` | 254 | Binds an active session's extension runtime to daemon clients, routing extension UI requests/dialogs and subagent host calls over the socket. | adapt |
| `docs/mcp-integrations.md` | 250 | MCP servers are exposed as Python-backed skills importable in the kernel, never as new model tools — keeps the tool surface at one. | adapt |
| `docs/providers.md` | 247 | OAuth/API-key credential resolution order and `auth.json` layout; credential plumbing, not governance. | reject |
| `src/core/rlm-runtime.ts` | 242 | Defines the subagent (RLM) runtime contract: spawn handles, a subagent registry with status, list/delete operations, and model search host handlers. | adopt |
| `docs/long-running-agents.md` | 241 | The single richest doc: agent-to-agent delivery modes, heartbeat surfaces, cron persistence, goals with `complete()`, autonomous bounds, compaction continuity. | adopt |
| `src/modes/daemon/daemon-worker-client.ts` | 241 | Client that connects a daemon supervisor to a session-worker process over a length-prefixed private framed socket and authenticates with a token. | adapt |
| `src/modes/interactive/components/bash-execution.ts` | 232 | TUI component streaming bash stdout/stderr with truncation and a spinner. | reject |
| `docs/packages.md` | 221 | npm/git distribution of extension+skill+prompt+theme bundles declared under a `pi` package.json key. | reject |
| `src/modes/daemon/command-recovery-journal.ts` | 217 | Append-only idempotency journal keyed by (clientId, commandId) with received/result/acknowledged records and periodic compaction. | adopt |
| `src/core/tools/output-accumulator.ts` | 216 | Accumulates streaming tool output under line/byte caps, spilling the full text to a temp file. | adopt |
| `src/utils/shell.ts` | 214 | Resolves a bash executable and shell env per platform, and sanitizes binary output for safe display. | adapt |
| `src/modes/daemon/compact-session-stream.ts` | 213 | Compacts assistant streaming events by stripping redundant full-message snapshots and reconstructs the full stream on the receiving side. | adapt |
| `examples/extensions/README.md` | 208 | Catalog of 50+ example extensions grouped by concern, plus the two key patterns: StringEnum params and state-in-`details` reconstruction. | adapt |
| `docs/keybindings.md` | 207 | Namespaced configurable keybinding IDs with automatic migration of legacy IDs; terminal-input concern, only the ID-migration idea generalizes. | reject |
| `examples/extensions/summarize.ts` | 206 | Summarizes the conversation with a different model and shows it transiently; the cheap-model-for-meta-work idea is transferable. | adapt |
| `src/core/mcp/mcp-manager.ts` | 205 | Host-side MCP integration: registers OAuth providers, gates integration skills behind auth state, and serves mcp.* host requests. | adapt |
| `src/modes/interactive/components/prime-team-selector.ts` | 203 | TUI fuzzy-filter menu for choosing a Prime Intellect team. | reject |
| `src/core/agent-observe.ts` | 200 | Read-only observation API letting an orchestrator list peer agents, their status/streaming/compacting state, and preview recent messages with size caps. | adopt |
| `src/modes/interactive/components/context-tree-format.ts` | 200 | Formats a context-usage tree into a width-constrained ANSI bar chart of token consumption per node. | reject |
| `src/modes/session-worker/private-framing.ts` | 200 | Length-prefixed binary frame codec (JSON header + payload) with header/payload size limits and a duplex channel wrapper. | adapt |
| `src/core/prompts/rlm.ts` | 199 | Builds the subagent/RLM system prompt sections: IPython control doctrine, recursion depth limits, and child-agent guidance. | adapt |
| `src/modes/agent-connection/snapshot.ts` | 197 | Builds an immutable snapshot of an agent connection's state: models, slash commands, resources, artifacts, and session recap. | adopt |
| `examples/extensions/interactive-shell.ts` | 196 | Suspends the TUI so interactive commands (vim, htop) get the real terminal, via the `user_bash` hook. | reject |
| `examples/extensions/truncated-tool.ts` | 194 | Mandatory output truncation for custom tools: 50KB/2000-line caps, full output spilled to a temp file, limits stated in the tool description. | adopt |
| `examples/extensions/handoff.ts` | 191 | Lossless alternative to compaction: LLM generates a self-contained prompt for a fresh session from the post-compaction branch slice. | adapt |
| `examples/extensions/github-issue-autocomplete.ts` | 185 | Stacks a custom autocomplete provider preloading `gh issue list` once per session then filtering locally; the preload-then-filter-locally idea is transferable. | reject |
| `src/core/system-prompt.ts` | 184 | Assembles the system prompt from custom/default body, tool list, skills, harness refinement state, and subagent guidance. | adapt |
| `src/utils/exif-orientation.ts` | 183 | Applies EXIF orientation transforms to decoded image data. | reject |
| `skills/websearch/src/websearch/websearch.py` | 178 | Serper-backed search skill reading its key from the host auth store on each call so mid-session credential adds work. | adapt |
| `src/utils/image-resize.ts` | 176 | Resizes/re-encodes an image to fit max dimensions and an encoded byte budget. | adapt |
| `examples/extensions/subagent/README.md` | 175 | Documents file-defined agent profiles (frontmatter name/description/tools/model), workflow presets, security model, and error/limit behavior. | adopt |
| `examples/extensions/doom-overlay/doom-engine.ts` | 173 | WebAssembly wrapper around doomgeneric exposing tick/create/keydown; demo only. | reject |
| `src/core/export-html/tool-renderer.ts` | 173 | Renders custom tool calls to HTML by invoking their TUI renderers and converting the ANSI output. | reject |
| `src/utils/version-check.ts` | 173 | Compares package versions and checks the registry for a newer release. | adapt |
| `src/modes/interactive/components/extension-selector.ts` | 172 | Generic TUI list selector with keyboard navigation and a countdown timer for extension prompts. | reject |
| `src/modes/print-mode.ts` | 172 | Single-shot non-interactive run mode: sends a prompt, streams text or JSON events, reports autonomous limit reasons, and exits with a code. | adapt |
| `src/modes/daemon/daemon-worker-protocol.ts` | 170 | Defines the supervisor↔worker wire protocol: env-var handshake, auth token requirement, startup gate fd, lifecycle states, and frame header validation. | adopt |
| `docs/agent-connection.md` | 169 | Client/execution boundary: UI may never own execution; serializable-only UI bridge, generation-aware cursors, journaled idempotent mutations, boundary invariants list. | adopt |
| `examples/extensions/plan-mode/utils.ts` | 168 | Pure, separately testable predicates: destructive-command regex denylist, `isSafeCommand`, plan-step extraction and completion marking. | adopt |
| `src/core/extensions/index.ts` | 168 | Re-export barrel for the extension system's runtime, runner, types, and builtin extensions. | n/a |
| `docs/quickstart.md` | 167 | Install-to-first-session walkthrough; onboarding prose with no transferable mechanism. | reject |
| `examples/extensions/ssh.ts` | 167 | Delegates edit and bash to a remote host through pluggable `BashOperations`, showing tool backends are swappable without changing the tool contract. | adapt |
| `docs/daemon.md` | 166 | Process topology, per-path session leases, due-tick claim-before-deliver scheduling, protocol v4 capability gating, two-phase coordinated updates, crash recovery. | adopt |
| `src/modes/interactive/components/injected-prompt-message.ts` | 165 | TUI component rendering system-injected prompt messages as collapsible markdown blocks. | reject |
| `src/core/compaction/utils.ts` | 164 | Tracks read/written/edited files across a conversation, serializes messages for summarization, and supplies the compaction summarization system prompt. | adopt |
| `src/modes/interactive/components/daxnuts.ts` | 164 | Easter egg rendering a hex-encoded 32x32 RGB tribute image in the terminal. | reject |
| `src/modes/interactive/components/conversation-components.ts` | 162 | Maps agent messages to the right TUI component per message type and merges adjacent compact messages. | reject |
| `src/core/side-question.ts` | 159 | Runs an out-of-band question against a throwaway sub-Agent seeded with conversation context, tracking running/complete/cancelled/error status. | adopt |
| `examples/extensions/built-in-tool-renderer.ts` | 158 | Re-registers built-in tools under the same name, delegating execution while replacing only the renderers — decorator-over-builtin pattern. | adapt |
| `src/modes/rpc/rpc-extension-ui-context.ts` | 157 | Bridges extension UI requests/responses over the RPC channel. | adapt |
| `src/core/bash-executor.ts` | 156 | Unified bash execution with streaming output, abort-signal cancellation, byte-capped tail truncation, and binary-output sanitization. | adapt |
| `src/modes/interactive/components/user-message-selector.ts` | 155 | TUI picker listing prior user messages for rewind/reuse. | reject |
| `src/modes/interactive/components/side-question.ts` | 152 | TUI component rendering side-question turns as markdown bubbles. | reject |
| `examples/extensions/border-status-editor.ts` | 150 | Custom editor component with a status-bearing border; terminal rendering only. | reject |
| `examples/extensions/overlay-test.ts` | 150 | Overlay compositing test with inline inputs and wide-char/emoji edge cases; rendering test fixture. | reject |
| `examples/extensions/minimal-mode.ts` | 148 | Overrides built-in tool renderers to hide output in collapsed mode; presentation-only. | reject |
| `examples/sdk/README.md` | 148 | Index of 13 progressively-scoped SDK examples from minimal defaults to full control with no discovery; good teaching-ladder shape. | adapt |
| `src/core/kernel/fork-server-script.ts` | 148 | Embedded Python forkserver source string implementing the newline-delimited JSON fork protocol and child reaping. | adapt |
| `src/core/session-resolver.ts` | 147 | Resolves a user-supplied session selector to a concrete session path, raising typed not-found and ambiguous errors with closest-match suggestions. | adopt |
| `src/modes/interactive/components/extension-editor.ts` | 147 | Multi-line TUI editor with Ctrl+G escape to an external $EDITOR via a temp file. | reject |
| `docs/rlm.md` | 145 | RLM invariants: one `ipython` tool, subagents as native calls returning admission handles only, skills as programmatic capability, explicit "not a security sandbox". | adopt |
| `src/core/agent-session-config.ts` | 144 | Defines the agent session runtime config shape and merges layered config (base, overrides, autonomous limits) into an effective config. | adopt |
| `src/modes/interactive/agent-activity.ts` | 144 | Derives a coarse activity state (thinking/writing/executing) and output-token count from the session event stream. | adapt |
| `src/core/resolve-config-value.ts` | 142 | Resolves config values that may be `!shell-command`, an env-var name, or a literal, with process-lifetime caching of command results. | adopt |
| `src/modes/agents-view/session-view-search.ts` | 142 | Parses a search query into fuzzy tokens, quoted phrases, or a regex and scores session text against it. | adapt |
| `examples/extensions/tools.ts` | 141 | Interactive tool enable/disable with selection persisted in session entries so it respects branch navigation. | adapt |
| `docs/sessions.md` | 139 | User-facing session lifecycle: continue/resume/fork/no-session flags and tree navigation; thin wrapper over session-format.md. | adapt |
| `examples/extensions/tool-override.ts` | 139 | Audited file reader wrapping a built-in tool to add logging and access control at the tool boundary rather than in the prompt. | adopt |
| `src/utils/photon.ts` | 139 | Loads the Photon WASM image library, patching readFileSync so compiled binaries find the .wasm. | reject |
| `src/modes/daemon/active-session-state.ts` | 137 | Tracks per-active-session daemon state: attached clients, event sequence numbers, backpressure catch-up sets, and pending extension UI requests. | adopt |
| `src/modes/interactive/feature-hints.ts` | 136 | Rotating deck of contextual feature hints gated by keybinding availability and session residency. | reject |
| `src/core/exec.ts` | 134 | Shared child-process command execution with abort signal, timeout, cwd, and captured stdout/stderr result. | adapt |
| `skills/skill-creator/references/python-skills.md` | 133 | The Python-backed skill detection contract: `pyproject.toml` presence, hyphen-to-underscore import name, exact `src/<name>/__init__.py`, silent degradation. | adopt |
| `examples/extensions/doom-overlay/doom-component.ts` | 132 | Renders DOOM frames as half-block characters with 24-bit color; terminal rendering demo. | reject |
| `src/modes/shared/startup-notices.ts` | 128 | Checks for app/extension package updates and tmux keyboard misconfiguration at startup and formats the notices. | reject |
| `examples/extensions/custom-compaction.ts` | 127 | Intercepts `session_before_compact` and returns a whole-conversation summary, discarding all old turns; shows the compaction override contract. | adapt |
| `src/utils/clipboard.ts` | 127 | Copies text to the system clipboard. | reject |
| `docs/termux.md` | 126 | Android/Termux install instructions; platform-specific setup. | reject |
| `examples/extensions/subagent/agents.ts` | 126 | Agent discovery with an explicit `AgentScope` of user/project/both — the trust boundary between personal and repo-controlled agent prompts. | adopt |
| `src/utils/child-process.ts` | 126 | Signals a process group or process and waits for child termination without hanging on inherited stdio handles. | adapt |
| `src/cli/daemon-list-format.ts` | 125 | Renders the session list as an aligned text table. | reject |
| `src/modes/interactive/components/centered-overlay.ts` | 125 | TUI overlay primitive that centers or full-panes a focusable component. | reject |
| `src/modes/interactive/components/collapsible-error.ts` | 125 | Normalizes and summarizes long error text and renders it collapsed/expanded in the terminal. | adapt |
| `examples/extensions/working-indicator.ts` | 123 | Customizes the inline streaming spinner; presentation-only. | reject |
| `src/modes/index.ts` | 123 | Re-export barrel for all run modes and their option types. | n/a |
| `examples/extensions/qna.ts` | 122 | Prompt-generator pattern: extract questions from the last assistant message into the editor for the user to answer. | reject |
| `src/modes/interactive/components/edit-summary.ts` | 120 | Extracts per-tool file-change summaries (added/removed lines) from tool results, merges them per turn, and renders the totals. | adapt |
| `examples/extensions/rpc-demo.ts` | 118 | Exercises every RPC-supported extension UI method as a protocol conformance fixture. | reject |
| `src/modes/headless-completion.ts` | 118 | Waits for a headless run to reach a terminal state, selecting the final assistant or slash-command result and reporting the autonomous limit reason. | adopt |
| `src/modes/rpc/jsonl.ts` | 117 | Serializes and frames newline-delimited JSON records with overflow-safe incremental line reading. | adopt |
| `src/modes/interactive/components/subagent-summary-line.ts` | 116 | Counts direct subagent statuses (running/idle) and renders a one-line TUI summary. | adapt |
| `src/modes/daemon/worker-recovery-journal.ts` | 114 | Append-only per-worker journal of last-known session busy/operation state, deduping unchanged records and compacting once all sessions are idle. | adopt |
| `src/modes/interactive/interactive-mode-services.ts` | 112 | Wires local UI-only services (theme, renderers, settings) that are deliberately kept out of the AgentConnection abstraction. | reject |
| `src/cli/list-models.ts` | 111 | Lists and fuzzy-filters available models for display. | reject |
| `src/modes/interactive/components/agent-message.ts` | 111 | Formats and renders inter-agent messages as summary lines, previews, and wrapped body text in the TUI. | reject |
| `docs/terminal-setup.md` | 106 | Per-terminal Kitty keyboard-protocol configuration snippets; pure terminal environment setup. | reject |
| `examples/extensions/doom-overlay/doom-keys.ts` | 104 | DOOM key-code constant table from doomkeys.h. | reject |
| `skills/rlm-heartbeat/src/rlm_heartbeat/__init__.py` | 102 | Thin async client over `rlm.host_request` for heartbeat create/list/update/delete — no policy in Python, all state in the host. | adopt |
| `src/cli/file-processor.ts` | 100 | Turns @file CLI arguments into prompt text plus image attachments. | adapt |

## packages/tui (24 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `src/components/editor.ts` | 2527 | Multi-line terminal editor with grapheme segmentation and paste-marker handling; reject — the paste-marker idea (large input replaced by a referenced handle) is the one transferable seed. | reject |
| `src/tui.ts` | 1933 | Differential-rendering TUI core diffing frames against the previous paint; reject — render loop, product-shape specific. | reject |
| `src/keys.ts` | 1408 | Kitty-protocol and legacy escape-sequence key parsing; reject — terminal input decoding. | reject |
| `src/utils.ts` | 1322 | Shared grapheme segmentation, width measurement and ANSI-aware string slicing; reject — text-measurement helpers for a monospace grid. | reject |
| `src/components/markdown.ts` | 1012 | Markdown-to-ANSI renderer with a strict strikethrough tokenizer and table-cell selection regions; reject — presentation only. | reject |
| `CHANGELOG.md` | 966 | UNREAD — release history for the TUI package. | n/a |
| `src/latex.ts` | 843 | Best-effort LaTeX-to-Unicode degradation that never renders worse than the raw source; reject — presentation, though graceful-degradation-with-a-floor is a good principle. | reject |
| `README.md` | 795 | UNREAD — terminal-UI component library reference, product-shape specific to a TS TUI. | n/a |
| `src/autocomplete.ts` | 790 | Terminal autocomplete provider stacking and path/word delimiter parsing; reject — pure terminal input rendering, no agent-governance content. | reject |
| `src/fullscreen.ts` | 705 | Alternate-screen viewport where scroll position is application state rather than terminal scrollback, diffed row-by-row; reject — rendering, but the "own your state, don't trust the substrate" framing is notable. | reject |
| `src/terminal.ts` | 614 | Raw-mode terminal lifecycle, OSC queries and progress keepalive; reject — terminal driver. | reject |
| `src/components/input.ts` | 507 | Single-line input with horizontal scrolling and grapheme-aware cursor; reject — terminal rendering. | reject |
| `src/terminal-image.ts` | 423 | Kitty/iTerm2 image protocol detection and cell-dimension math with fallback; reject — capability negotiation for pixels. | reject |
| `src/stdin-buffer.ts` | 411 | Buffers partial stdin chunks so split escape sequences are not misread as keypresses; reject — but the "never parse a partial frame" rule generalizes to any streaming protocol. | reject |
| `src/components/select-list.ts` | 309 | Two-column selection list with description truncation; reject — terminal widget. | reject |
| `src/keybindings.ts` | 265 | Global keybinding registry extended by downstream packages via TS declaration merging; reject — the typed-registry-extended-by-consumers pattern is the only transferable seed. | reject |
| `src/components/settings-list.ts` | 250 | Label/description/current-value settings widget; reject — terminal widget, though the id/label/description/currentValue shape is a decent settings-schema sketch. | reject |
| `src/terminal-colors.ts` | 206 | RGB parsing of OSC default foreground/background query responses; reject — terminal capability detection. | reject |
| `src/selection-metadata.ts` | 182 | Encodes table-cell selection regions as invisible OSC markers inside rendered text; reject — out-of-band metadata smuggled through a text channel, terminal-specific. | reject |
| `src/components/box.ts` | 159 | Bordered box component with a render cache keyed on width and background sample; reject — terminal layout primitive. | reject |
| `src/index.ts` | 152 | Barrel re-exporting the TUI public surface; reject — module wiring for a terminal library. | reject |
| `src/fuzzy.ts` | 150 | Subsequence fuzzy matcher with lower-is-better scoring; reject — generic string utility, trivially replaceable in Python. | reject |
| `src/components/image.ts` | 135 | Image component with fullscreen fallback over terminal image protocols; reject — terminal rendering. | reject |
| `src/components/text.ts` | 106 | Word-wrapping multi-line text component with padding; reject — terminal primitive. | reject |

## prime-agent-runtime/src (3 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `rlm/harness.py` | 819 | Continual Harness CRUD store: HarnessEntry/RefinementEvent, mtime _sync_from_disk, corrupt-file tolerance, None-preserves field semantics. | adopt |
| `rlm/__init__.py` | 346 | Kernel-side shim exposing the typed host-request bridge, RLM subagent spawn/list/delete API, and a fork-safe harness state proxy. | adopt |
| `rlm/mcp_base.py` | 331 | Base class for MCP integrations: credential resolution/refresh, transport capability negotiation, lazy tool discovery, and result normalization. | adopt |

## scripts/cost.ts (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/cost.ts` | 183 | Aggregates per-session token cost from JSONL transcripts over a directory and day window. | adapt |

## scripts/render-logo.py (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/render-logo.py` | 117 | Renders an SVG logo to half-block ASCII using uv `--with` so no venv is committed. | reject |

## scripts/session-transcripts.ts (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/session-transcripts.ts` | 406 | Extracts transcripts for a cwd, splits them into context-sized files, and optionally spawns subagents to analyze patterns across them. | adopt |

## scripts/stats.ts (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/stats.ts` | 233 | Aggregates token/cost usage across sessions from raw JSONL entries. | adapt |

## scripts/tool-stats.ts (1 files)

| file | loc | responsibility | verdict |
|---|---:|---|---|
| `scripts/tool-stats.ts` | 232 | Per-tool call/result/error counts and estimated token cost mined from transcripts. | adopt |
